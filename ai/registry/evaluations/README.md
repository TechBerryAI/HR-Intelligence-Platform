# Evaluation registry

Immutable records for evaluation runs (`EVAL-{CAT}-{YYYYMMDD}`).

Links to `evaluation/reports/` and promotion decisions.

See [registry/README.md](../README.md#evaluation-registry-example).
