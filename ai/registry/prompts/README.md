# Prompt registry

Version history for all prompt templates (`PROMPT-NNNN`).

Active prompts live in `prompts/`. This registry records every version, changelog, and downstream usage (datasets, models, evaluations).

See [registry/README.md](../README.md#prompt-registry-example).
