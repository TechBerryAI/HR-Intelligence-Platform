# Deployment registry

Deployment snapshots (`DEPLOY-{feature}-v{N}-{target}`).

Tracks Ollama tags, Modelfiles, and health checks. Use immutable tags — not `latest`.

See [registry/README.md](../README.md#deployment-registry-example).
