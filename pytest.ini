[pytest]
testpaths = tests/backend
pythonpath = apps/backend
markers =
    integration: tests that require external services (Ollama, database)
    ocr: live OCR tests that require RapidOCR or Tesseract
