# schema_pg — FROZEN BASELINE

These SQL files (`01_core.sql` … `04_seeds.sql`) are applied **only** by Alembic
revisions `20260806_0001` and `20260806_0002`.

**Do not add feature DDL here.** Create a new Alembic revision under
`alembic/versions/` instead. See `alembic/README.md`.
