# Alembic baseline SQL

Frozen snapshot applied **only** by revision `20260810_s001`.

| File | Role |
|------|------|
| `001_schema.sql` | Full public schema (tables, constraints, indexes, triggers, functions) |
| `002_seeds.sql` | Seed HEAD_HR / CEO accounts (env-gated passwords) |

**Do not edit these for feature work.** Add a new Alembic revision under `alembic/versions/` instead.
