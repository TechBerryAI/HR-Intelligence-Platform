# Backend requires Python 3.10–3.12 recommended (3.9 works if modules use
# `from __future__ import annotations` — required for `str | None` type hints).
# Python 3.13+ can run the API; RapidOCR is skipped (no wheels yet) — scanned-image OCR
# then needs system Tesseract, or use Python 3.12 for pip RapidOCR.
Flask==3.0.3
Flask-Cors==4.0.0
Flask-Mail==0.9.1
PyJWT==2.9.0
bcrypt==4.2.0
psycopg[binary]>=3.1
redis>=5.0.0
python-dotenv==1.0.1
SQLAlchemy==2.0.36
alembic>=1.13
cryptography>=42.0.0
requests==2.31.0
PyPDF2==3.0.1
pymupdf>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0
# RapidOCR has no Python 3.13+ wheels; install only when supported
rapidocr-onnxruntime>=1.3.24; python_version < "3.13"
numpy>=1.24.0
opencv-python-headless>=4.8.0
# Optional DocLayout-YOLO (torch); heuristic OpenCV layout works without it
# doclayout-yolo>=0.0.2
python-docx==1.1.0
python-dateutil==2.9.0.post0
openpyxl==3.1.2
gunicorn>=21.0

# AI Runtime gateway (Ollama / Provider Manager)
-r ../../ai/requirements-runtime.txt

# IMPORTANT: Do not remove any packages above
# All are required for the application to work correctly
