# Packages

Shared helpers used by the backend/AI layers.

| Package | Role |
|---------|------|
| `knowledge/` | Knowledge-base directory path helpers (`packages.knowledge`) |

CLI utilities live under `scripts/` and `ai/`.
